feat(phase13.3): Community features — pet sharing, gallery, leaderboard

- Community.h/cpp: Pet card creation, gallery management, leaderboard sorting
- Pet profile sharing via JSON (shareable profile with stats, achievements, lineage)
- Gallery: add/remove/update entries, max 20 entries, persistent in SPIFFS
- Leaderboard: sort by achievements, age, or generation with configurable limit
- Import: parse shared pet cards into gallery
- Web API: GET /api/community/gallery, /leaderboard, POST /share, /import
- Rate limiting on all community endpoints
- 11 unit tests covering card creation, JSON serialization, gallery, leaderboard, scoring, import
- Test infrastructure: SPIFFS.h mock, Arduino.h improvements (HEX/DEC/OCT/BIN, toUpperCase, getEfuseMac)
